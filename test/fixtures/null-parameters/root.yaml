openapi: 3.1.0
info:
  title: Null Parameters API
  version: 1.0.0
paths:
  /organizations/{organizationId}/clients/{clientId}:
    parameters:
      - name: organizationId
        in: path
        schema:
          type: string
      - name: clientId
        in: path
        schema:
          type: string
    get:
      operationId: GetApiClient
      summary: Get an API Client by clientId
      responses:
        '200':
          description: ok
    patch:
      operationId: UpdateApiClient
      parameters: null
      summary: Update an API Client
      responses:
        '200':
          description: ok
